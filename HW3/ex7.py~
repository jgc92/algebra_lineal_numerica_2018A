import numpy as np

def fie(a,b,f,K,h):
    m = int((b-a)/h)
    x = np.zeros(m+1)

    for i in range(m+1):
        x[i] = a + i*h
    # Construct coeffs matrix
    for i in range(m+1):
        for j in range(m+1):
            K_matrix[i,j] = K(x[i],t[j])
